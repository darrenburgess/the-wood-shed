[build]
  # Build command to run on every deployment
  command = "npm run build"

  # Directory to publish (where the built files are)
  publish = "dist"

[build.environment]
  # Specify Node.js version
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
